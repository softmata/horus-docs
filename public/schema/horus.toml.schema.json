{
  "$id": "https://docs.horus-registry.dev/schema/horus.toml.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "HorusManifest",
  "description": "The typed representation of a `horus.toml` manifest.\n\nSingle source of truth for a horus project: metadata, dependencies,\ndrivers, scripts, ignore patterns, and feature flags.",
  "type": "object",
  "properties": {
    "cpp": {
      "description": "`[cpp]` -- C++ build configuration (compiler override, cmake args, toolchain).",
      "anyOf": [
        {
          "$ref": "#/$defs/CppConfig"
        },
        {
          "type": "null"
        }
      ]
    },
    "dependencies": {
      "description": "`[dependencies]` -- project dependencies (all sources).",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/DependencyValue"
      }
    },
    "dev-dependencies": {
      "description": "`[dev-dependencies]` -- dev-only dependencies (not included in publish/deploy).",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/DependencyValue"
      }
    },
    "drivers": {
      "description": "`[drivers]` -- legacy hardware driver configuration (use [hardware] instead).",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/DriverValue"
      }
    },
    "enable": {
      "description": "`enable = [\"cuda\", \"editor\"]` -- capabilities to enable.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "hardware": {
      "description": "`[hardware]` -- hardware node configuration (preferred).\nFalls back to `[drivers]` for backward compatibility.",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/DriverValue"
      }
    },
    "hooks": {
      "description": "`[hooks]` -- pre/post action hooks for run/build/test.",
      "$ref": "#/$defs/HooksConfig"
    },
    "ignore": {
      "description": "`[ignore]` -- patterns to exclude during scanning.",
      "$ref": "#/$defs/IgnoreConfig"
    },
    "network": {
      "description": "`[network]` -- LAN replication configuration (optional).\nWhen present, configures horus_net for cross-machine topic sharing.",
      "anyOf": [
        {
          "$ref": "#/$defs/NetworkConfig"
        },
        {
          "type": "null"
        }
      ]
    },
    "package": {
      "description": "`[package]` -- project metadata. Required for single-package projects.\nFor virtual workspaces (only `[workspace]`), this defaults to empty.",
      "$ref": "#/$defs/PackageInfo",
      "default": {
        "name": "",
        "version": "0.0.0"
      }
    },
    "robot": {
      "description": "`[robot]` -- robot model configuration (optional).",
      "anyOf": [
        {
          "$ref": "#/$defs/RobotConfig"
        },
        {
          "type": "null"
        }
      ]
    },
    "rust": {
      "description": "`[rust]` -- Cargo settings spliced into the generated `.horus/Cargo.toml`.",
      "anyOf": [
        {
          "$ref": "#/$defs/RustConfig"
        },
        {
          "type": "null"
        }
      ]
    },
    "scripts": {
      "description": "`[scripts]` -- custom project commands (like npm scripts / just).",
      "type": "object",
      "additionalProperties": {
        "type": "string"
      }
    },
    "sim-dependencies": {
      "description": "`[sim-dependencies]` -- simulation asset dependencies, installed by the\nsimulation tooling. (There is no `horus sim` subcommand in this CLI; the\ndoc named one that does not exist.)\n\nPackage types: robot, world, sensor-preset, task, actuator-preset.\nResolved from horus-registry and cached in `.horus/packages/`.",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/DependencyValue"
      }
    },
    "sim-drivers": {
      "description": "`[sim-drivers]` -- legacy simulation overrides (use `sim = true` in [hardware] instead).",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/DriverValue"
      }
    },
    "workspace": {
      "description": "`[workspace]` -- multi-crate workspace configuration (optional).\nWhen present, this manifest is a workspace root.",
      "anyOf": [
        {
          "$ref": "#/$defs/WorkspaceConfig"
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "$defs": {
    "CppConfig": {
      "description": "C++ build configuration under `[cpp]` in horus.toml.",
      "type": "object",
      "properties": {
        "cmake_args": {
          "description": "Additional CMake arguments injected into the generated CMakeLists.txt.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "compiler": {
          "description": "Override the C++ compiler (e.g., `\"clang++\"`).",
          "type": [
            "string",
            "null"
          ]
        },
        "toolchain": {
          "description": "Cross-compilation toolchain target (e.g., `\"aarch64\"`, `\"armv7\"`).",
          "type": [
            "string",
            "null"
          ]
        }
      }
    },
    "DepSource": {
      "description": "Source of a dependency.",
      "oneOf": [
        {
          "description": "Horus registry (default when no source specified).",
          "type": "string",
          "const": "registry"
        },
        {
          "description": "Rust crates from crates.io.",
          "type": "string",
          "const": "crates.io"
        },
        {
          "description": "Python packages from PyPI.",
          "type": "string",
          "const": "pypi"
        },
        {
          "description": "System package (apt, brew, etc.).",
          "type": "string",
          "const": "system"
        },
        {
          "description": "Local path dependency.",
          "type": "string",
          "const": "path"
        },
        {
          "description": "Git repository dependency.",
          "type": "string",
          "const": "git"
        }
      ]
    },
    "DependencyValue": {
      "description": "Dependency value: either a simple version string or a detailed table.\n\n- Simple: `serde = \"1.0\"` → `DependencyValue::Simple(\"1.0\")`\n- Detailed: `serde = { version = \"1.0\", features = [\"derive\"] }` → `DependencyValue::Detailed(...)`",
      "anyOf": [
        {
          "description": "Version string only (e.g., `\"1.0\"`, `\">=1.24\"`, `\"^2.0\"`).\nDefaults to horus registry source.",
          "type": "string"
        },
        {
          "description": "Full dependency specification.",
          "$ref": "#/$defs/DetailedDependency"
        }
      ]
    },
    "DetailedDependency": {
      "description": "Detailed dependency specification.\n\nUsed when a dependency needs more than just a version string.",
      "type": "object",
      "properties": {
        "apt": {
          "description": "Apt package name for system dependencies (e.g., `\"libeigen3-dev\"`).",
          "type": [
            "string",
            "null"
          ]
        },
        "branch": {
          "description": "Git branch (for source = \"git\").",
          "type": [
            "string",
            "null"
          ]
        },
        "cmake_package": {
          "description": "CMake package name for `find_package()` (e.g., `\"Eigen3\"`).",
          "type": [
            "string",
            "null"
          ]
        },
        "features": {
          "description": "Cargo/Python features to enable.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "git": {
          "description": "Git repository URL (for source = \"git\").",
          "type": [
            "string",
            "null"
          ]
        },
        "lang": {
          "description": "Language this dependency belongs to (e.g., `\"cpp\"`, `\"rust\"`, `\"python\"`).",
          "type": [
            "string",
            "null"
          ]
        },
        "optional": {
          "description": "Whether this dependency is optional.",
          "type": "boolean"
        },
        "path": {
          "description": "Local path (for source = \"path\").",
          "type": [
            "string",
            "null"
          ]
        },
        "rev": {
          "description": "Git revision (for source = \"git\").",
          "type": [
            "string",
            "null"
          ]
        },
        "source": {
          "description": "Dependency source.",
          "anyOf": [
            {
              "$ref": "#/$defs/DepSource"
            },
            {
              "type": "null"
            }
          ]
        },
        "tag": {
          "description": "Git tag (for source = \"git\").",
          "type": [
            "string",
            "null"
          ]
        },
        "version": {
          "description": "Version requirement (e.g., \"1.0\", \">=1.24\", \"^2.0\").",
          "type": [
            "string",
            "null"
          ]
        },
        "workspace": {
          "description": "Inherit version/source/features from `[workspace.dependencies]`.",
          "type": "boolean"
        }
      }
    },
    "DriverTableConfig": {
      "description": "Structured driver configuration from a `[drivers.NAME]` TOML table.\n\nExactly one of `terra`, `package`, or `node` should be present to identify\nthe driver source. All other keys are captured in `params` and passed to\nthe driver factory at runtime.\n\n# Examples\n\n```toml\n# Terra driver (pre-built hardware support)\n[drivers.arm]\nterra = \"dynamixel\"\nport = \"/dev/ttyUSB0\"\nbaudrate = 1000000\nservo_ids = [1, 2, 3, 4, 5, 6]\n\n# Registry package (community/vendor driver)\n[drivers.force_sensor]\npackage = \"horus-driver-ati-netft\"\naddress = \"192.168.1.100\"\n\n# Local code (user's own driver registered via register_driver!)\n[drivers.conveyor]\nnode = \"ConveyorDriver\"\nport = \"/dev/ttyACM0\"\nbaudrate = 57600\n```",
      "type": "object",
      "properties": {
        "args": {
          "description": "CLI arguments for exec drivers.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        },
        "crate": {
          "description": "Rust crate name from crates.io — `crate = \"rplidar-driver\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "exec": {
          "description": "External binary path — `exec = \"./realsense_bridge\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "node": {
          "description": "Local node struct name registered via `register_driver!` (e.g., `\"ConveyorDriver\"`).",
          "type": [
            "string",
            "null"
          ]
        },
        "package": {
          "description": "Registry package name (e.g., `\"horus-driver-ati-netft\"`).",
          "type": [
            "string",
            "null"
          ]
        },
        "pip": {
          "description": "Python package name from PyPI — `pip = \"adafruit-bno055\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "sim": {
          "description": "Simulation flag — when true, replaced by sim stub when `horus run --sim`.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "simulated": {
          "description": "Simulated by a simulator plugin — `simulated = true`.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "source": {
          "description": "Crate source identifier (e.g., `\"crates-io\"`). Implied when `crate` is present.",
          "type": [
            "string",
            "null"
          ]
        },
        "terra": {
          "description": "Terra driver shortname (legacy — use `use` instead).",
          "type": [
            "string",
            "null"
          ]
        },
        "topic": {
          "description": "HORUS topic for sensor data output (e.g., `\"sensors/imu\"`).",
          "type": [
            "string",
            "null"
          ]
        },
        "topic_command": {
          "description": "HORUS topic for command input (e.g., `\"arm/joint_commands\"`).",
          "type": [
            "string",
            "null"
          ]
        },
        "topic_state": {
          "description": "HORUS topic for joint/state output (e.g., `\"arm/joint_states\"`).",
          "type": [
            "string",
            "null"
          ]
        },
        "use": {
          "description": "Node type name — the new unified source key (e.g., `\"dynamixel\"`, `\"rplidar\"`).\nReplaces the 6 legacy keys (terra, package, node, crate, pip, exec).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": true
    },
    "DriverValue": {
      "description": "Driver configuration value.\n\nThree forms:\n- Config table: `[drivers.arm]` with `terra`/`package`/`node` key + params\n- Simple string: `camera = \"opencv\"` (backend name → feature flags)\n- Enable bool: `camera = true` (enable with default backend)\n\nThe `Config` variant must be first — `#[serde(untagged)]` tries variants\nin order, and TOML tables must match before scalar strings/bools.",
      "anyOf": [
        {
          "description": "Config table: `[drivers.arm]` with terra/package/node key + params.",
          "$ref": "#/$defs/DriverTableConfig"
        },
        {
          "description": "Backend name string, e.g., `\"opencv\"`.",
          "type": "string"
        },
        {
          "description": "Enable with default backend.",
          "type": "boolean"
        }
      ]
    },
    "HooksConfig": {
      "description": "Pre/post action hooks that run automatically before/after commands.",
      "type": "object",
      "properties": {
        "post_build": {
          "description": "Hooks to run after `horus build`.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "post_run": {
          "description": "Hooks to run after `horus run` exits.\n\nOnly `test` had a post hook, so the one point a robotics user most\nwants — teardown after a run, to release hardware, park an arm or\npower down a bus — did not exist, and nothing explained the asymmetry.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "post_test": {
          "description": "Hooks to run after `horus test`.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "pre_build": {
          "description": "Hooks to run before `horus build`.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "pre_run": {
          "description": "Hooks to run before `horus run` (e.g., `[\"fmt\", \"lint\"]`).",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "pre_test": {
          "description": "Hooks to run before `horus test`.",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "IgnoreConfig": {
      "description": "Patterns to exclude during file scanning.",
      "type": "object",
      "properties": {
        "directories": {
          "description": "Directory names to ignore (e.g., `[\"old/\", \"experiments/\"]`).",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "files": {
          "description": "File glob patterns to ignore (e.g., `[\"debug_*.py\", \"**/experiments/**\"]`).",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "packages": {
          "description": "Package names to skip during auto-install (e.g., `[\"ipython\"]`).",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "NetworkConfig": {
      "description": "Network replication configuration for horus_net.\n\nParsed from `[network]` in horus.toml. All fields optional — defaults are\nsensible for zero-config LAN operation.",
      "type": "object",
      "properties": {
        "deny_export": {
          "description": "Export deny patterns (e.g., [\"camera.*\", \"debug.*\"]).",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        },
        "enabled": {
          "description": "Enable/disable networking. Default: true. Env override: HORUS_NO_NETWORK=1.",
          "type": [
            "boolean",
            "null"
          ],
          "default": null
        },
        "import": {
          "description": "Import control: \"auto\" (default), \"deny\", or list of topic patterns.\n\n`toml::Value` has no `JsonSchema` impl, so under the `schema` feature it\nis described as an arbitrary JSON value — which is accurate: this field\nis deliberately polymorphic (a string or a list of patterns)."
        },
        "optimize": {
          "description": "Enabled optimizers (e.g., [\"fusion\", \"delta\"]).",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        },
        "safety": {
          "description": "Safety heartbeat settings.",
          "anyOf": [
            {
              "$ref": "#/$defs/NetworkSafetyConfig"
            },
            {
              "type": "null"
            }
          ]
        },
        "secret": {
          "description": "Shared secret for peer filtering (NOT security).",
          "type": [
            "string",
            "null"
          ]
        }
      }
    },
    "NetworkSafetyConfig": {
      "description": "Safety heartbeat configuration within [network].",
      "type": "object",
      "properties": {
        "heartbeat_ms": {
          "description": "Heartbeat interval in ms (default: 50).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "missed_threshold": {
          "description": "Missed heartbeats before link-dead (default: 3).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "on_link_lost": {
          "description": "Action on link loss: \"warn\", \"safe_state\", \"stop\" (default: \"warn\").",
          "type": [
            "string",
            "null"
          ]
        }
      }
    },
    "PackageInfo": {
      "description": "Project metadata under `[package]`.",
      "type": "object",
      "properties": {
        "authors": {
          "description": "List of authors (e.g., `[\"Jane Doe <jane@example.com>\"]`).",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "categories": {
          "description": "Registry categories (comma-separated or array).",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "description": {
          "description": "Human-readable description.",
          "type": [
            "string",
            "null"
          ]
        },
        "edition": {
          "description": "Manifest schema edition for forward compatibility.",
          "type": "string"
        },
        "license": {
          "description": "SPDX license identifier (e.g., `\"Apache-2.0\"`).",
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "Package name (2-64 chars, lowercase alphanumeric + hyphens/underscores).",
          "type": "string"
        },
        "package-type": {
          "description": "Package type for the registry.",
          "anyOf": [
            {
              "$ref": "#/$defs/PackageType"
            },
            {
              "type": "null"
            }
          ]
        },
        "repository": {
          "description": "Source code repository URL.",
          "type": [
            "string",
            "null"
          ]
        },
        "rust_edition": {
          "description": "Rust edition override (e.g., `\"2021\"`). Used by cargo_gen.",
          "type": [
            "string",
            "null"
          ]
        },
        "standard": {
          "description": "C++ language standard (e.g., `\"c++17\"`, `\"c++20\"`, `\"c++23\"`).",
          "type": [
            "string",
            "null"
          ]
        },
        "type": {
          "description": "Crate target type: \"bin\" (default), \"lib\", or \"both\".",
          "$ref": "#/$defs/TargetType"
        },
        "version": {
          "description": "Semantic version string (e.g., `\"0.1.0\"`).",
          "type": "string"
        }
      },
      "required": [
        "name",
        "version"
      ]
    },
    "PackageType": {
      "description": "Package type for registry classification.",
      "type": "string",
      "enum": [
        "node",
        "driver",
        "tool",
        "algorithm",
        "model",
        "message",
        "app"
      ]
    },
    "RobotConfig": {
      "description": "Robot model configuration under `[robot]`.",
      "type": "object",
      "properties": {
        "description": {
          "description": "Path to URDF/Xacro file, relative to project root.",
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "Robot name used in topic naming (e.g., `\"turtlebot\"`).",
          "type": "string"
        },
        "simulator": {
          "description": "Simulator plugin to use with `horus run --sim`.\nWhen absent, defaults to `\"sim3d\"` at runtime.\nExamples: `\"sim3d\"`, `\"mujoco\"`, `\"isaac\"`, `\"gazebo\"`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "name"
      ]
    },
    "RustConfig": {
      "description": "`[rust]` — Rust/Cargo build configuration in horus.toml.\n\n`.horus/Cargo.toml` is generated from `horus.toml` and rewritten whenever\n`horus.toml` is newer, so anything added to it by hand is destroyed on the\nnext build. `[cpp]` has forwarded settings to CMake all along; Rust had no\nequivalent, which left a cargo feature, a profile setting or an extra patch\nwith no supported way to be expressed. The documentation's own answer was to\nedit the generated file.\n\nContents are merged verbatim into the generated manifest — all but\n`rustflags`, which Cargo.toml cannot express at all and which goes to a\ngenerated cargo config file beside it instead. Kept to the sections HORUS\ndoes not itself write, so there is nothing to conflict with:\n`[dependencies]` is deliberately absent, because `horus.toml` already has a\n`[dependencies]` table that `horus cargo add` round-trips through, and a\nsecond channel would let the same crate be written twice.",
      "type": "object",
      "properties": {
        "build-dependencies": {
          "description": "`[build-dependencies]`."
        },
        "edition": {
          "description": "Rust edition for the generated package. Overrides `[package].rust_edition`.",
          "type": [
            "string",
            "null"
          ]
        },
        "features": {
          "description": "`[features]` — name to list of enabled features."
        },
        "lints": {
          "description": "`[lints]`."
        },
        "patch": {
          "description": "`[patch.*]` — merged with the patches HORUS emits for its own git\nsources. HORUS's entries win a key collision, with a warning."
        },
        "profile": {
          "description": "`[profile.*]` — `opt-level`, `lto`, `debug`, `panic`, and friends."
        },
        "rustflags": {
          "description": "`rustflags` — flags handed to every `rustc` invocation for this project.\n\nThe odd one out in this struct: everything above is a Cargo.toml\nsection and rides the passthrough into the generated manifest, but\nCargo.toml has no rustflags slot at all. The only homes cargo offers are\nthe `RUSTFLAGS` environment variable and `[build] rustflags` in a cargo\n*config* file, so this one is emitted to a generated\n`.horus/.cargo/config.toml` instead — see\n`cargo_gen::write_cargo_config` for why that location is the one cargo\nfinds. An exported `RUSTFLAGS` still wins, because that is cargo's own\nprecedence and quietly overriding a variable the user set by hand would\nbe the worse surprise.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "target": {
          "description": "`[target.*]` — per-target dependencies and settings."
        }
      }
    },
    "TargetType": {
      "description": "Crate target type.",
      "type": "string",
      "enum": [
        "bin",
        "lib",
        "both"
      ]
    },
    "WorkspaceConfig": {
      "description": "Multi-crate workspace configuration under `[workspace]`.",
      "type": "object",
      "properties": {
        "dependencies": {
          "description": "Shared dependencies inherited by members via `workspace = true`.",
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/DependencyValue"
          }
        },
        "exclude": {
          "description": "Glob patterns to exclude from membership.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "members": {
          "description": "Glob patterns for workspace members (e.g., `[\"crates/*\"]`).",
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        }
      }
    }
  }
}