---
title: "Error Handling"
description: "Unified error types, result handling, and best practices for HORUS applications"
weight: 70
---

# Error Handling

HORUS provides a unified error handling system built on Rust's `Result` type, with rich error contexts and helpful diagnostics.

## Quick Start

```rust
use horus::prelude::*;

fn my_function() -> Result<()> {
    // Your code here
    Ok(())
}
```

The prelude exports these error types:
- `HorusError` - The main error enum
- `HorusResult<T>` - Alias for `std::result::Result<T, HorusError>`
- `Result<T>` - Short alias for `HorusResult<T>`

## Core Error Types

### HorusError

The main error type for all HORUS operations:

### Error Variants

| Variant | Description |
|---------|-------------|
| `Io` | File system and I/O errors (`#[from] std::io::Error`) |
| `Config` | Configuration parsing/validation |
| `Communication` | IPC and network errors |
| `Node { node, message }` | Node-specific errors (named fields) |
| `Memory` | Memory allocation errors |
| `Serialization` | Serialization/deserialization errors |
| `NotFound` | Resource not found |
| `PermissionDenied` | Access denied |
| `InvalidInput` | Invalid argument/input |
| `AlreadyExists` | Resource already exists |
| `Parse` | Parsing errors |
| `Unsupported` | Operation not supported on this platform |
| `Internal { message, file, line }` | Internal errors with source location |
| `Contextual { message, source }` | Error with preserved source chain |

## Creating Errors

### Using Constructors

`HorusError` provides convenience constructors for the most common variants:

```rust
use horus::prelude::*;

// Configuration error
let err = HorusError::config("Invalid frequency: must be positive");

// Node error with context (takes node name + message)
let err = HorusError::node("MotorController", "Failed to initialize PWM");

// Communication error
let err = HorusError::communication("Topic not found: /cmd_vel");

// Memory error
let err = HorusError::memory("Failed to allocate 1GB for buffer");

// Invalid input (use variant directly)
let err = HorusError::InvalidInput("Speed must be between 0 and 100".to_string());

// Internal error (prefer horus_internal! macro for file/line capture)
let err = horus_internal!("Unexpected state reached");
```

### Using Variants Directly

```rust
use horus::prelude::*;

let err = HorusError::PermissionDenied("/dev/ttyUSB0".to_string());
let err = HorusError::AlreadyExists("Session 'main' already exists".to_string());
let err = HorusError::Parse("Invalid YAML syntax at line 5".to_string());
```

### Internal Errors with Source Location

Use the `horus_internal!()` macro to create internal errors that automatically capture file and line number:

```rust
use horus::prelude::*;

// Captures file/line automatically
return Err(horus_internal!("Unexpected state: {:?}", state));
// Produces: Internal { message: "Unexpected state: ...", file: "src/foo.rs", line: 42 }
```

### Contextual Errors with Source Chain

Use `HorusError::Contextual` to wrap errors with additional context while preserving the original error chain:

```rust
use horus::prelude::*;

let config = load_file("robot.yaml")
    .map_err(|e| HorusError::Contextual {
        message: "Failed to load robot configuration".to_string(),
        source: Box::new(e),
    })?;
// Produces: "Failed to load robot configuration\n  Caused by: <original error>"
```

## Error Propagation

### Using the `?` Operator

```rust
use horus::prelude::*;

fn load_robot_config(path: &str) -> Result<Config> {
    // File I/O errors automatically convert to HorusError::Io
    let content = std::fs::read_to_string(path)?;

    // JSON errors automatically convert to HorusError::Serialization
    let config: Config = serde_json::from_str(&content)?;

    Ok(config)
}
```

### Automatic Conversions

`HorusError` implements `From` for many common error types:

| Source Type | Target Variant |
|-------------|----------------|
| `std::io::Error` | `HorusError::Io` |
| `serde_json::Error` | `HorusError::Serialization` |
| `serde_yaml::Error` | `HorusError::Serialization` |
| `toml::de::Error` | `HorusError::Config` |
| `toml::ser::Error` | `HorusError::Serialization` |
| `std::num::ParseIntError` | `HorusError::Parse` |
| `std::num::ParseFloatError` | `HorusError::Parse` |
| `std::str::ParseBoolError` | `HorusError::Parse` |
| `uuid::Error` | `HorusError::Internal` |
| `std::sync::PoisonError<T>` | `HorusError::Internal` |
| `Box<dyn std::error::Error>` | `HorusError::Internal` |
| `Box<dyn std::error::Error + Send + Sync>` | `HorusError::Contextual` |
| `anyhow::Error` | `HorusError::Internal` |

## Error Checking

### Pattern Matching

```rust
use horus::prelude::*;

match result {
    Ok(value) => process(value),
    Err(HorusError::NotFound(resource)) => {
        eprintln!("Resource not found: {}", resource);
    }
    Err(HorusError::Node { node, message }) => {
        eprintln!("Node {} error: {}", node, message);
    }
    Err(HorusError::Internal { message, file, line }) => {
        eprintln!("Internal error at {}:{}: {}", file, line, message);
    }
    Err(e) => {
        eprintln!("Unexpected error: {}", e);
    }
}
```

## Best Practices

### 1. Use Specific Error Types

```rust
// Good: Specific error with context
return Err(HorusError::node("IMU", "I2C read failed on register 0x3B"));

// Avoid: Internal without context
return Err(horus_internal!("something went wrong"));
```

### 2. Add Context When Propagating

```rust
fn initialize_sensor() -> Result<()> {
    open_i2c_bus().map_err(|e| {
        HorusError::node("IMU", format!("Failed to open I2C: {}", e))
    })?;

    Ok(())
}
```

### 3. Handle Expected Errors Gracefully

```rust
fn get_config() -> Result<Config> {
    match load_config_file("config.yaml") {
        Ok(config) => Ok(config),
        Err(HorusError::NotFound(_)) => {
            // Expected: use defaults
            Ok(Config::default())
        }
        Err(e) => Err(e),  // Propagate unexpected errors
    }
}
```

### 4. Log Errors Before Propagating

```rust
use horus::prelude::*;

fn critical_operation() -> Result<()> {
    match do_something_important() {
        Ok(result) => Ok(result),
        Err(e) => {
            hlog!(error, "Critical operation failed: {}", e);
            Err(e)
        }
    }
}
```

## Node Error Handling

### In Tick Methods

```rust
impl Node for MyNode {
    fn tick(&mut self) {
        // Handle errors in tick - don't propagate
        if let Err(e) = self.process_data() {
            hlog!(error, "Processing failed: {}", e);
            // Optionally publish status
            self.publish_error_status(e);
        }
    }
}
```

### Initialization Errors

```rust
impl MyNode {
    pub fn new(config: Config) -> Result<Self> {
        let driver = config.driver.connect().map_err(|e| {
            HorusError::node("MyNode", format!("Driver init failed: {}", e))
        })?;

        Ok(Self { driver })
    }
}
```

### Graceful Degradation

```rust
fn read_sensor(&mut self) -> Option<SensorData> {
    match self.backend.read() {
        Ok(data) => Some(data),
        Err(e) => {
            self.error_count += 1;
            if self.error_count > 10 {
                hlog!(error, "Sensor failing repeatedly: {}", e);
            }
            None  // Return None instead of crashing
        }
    }
}
```

## Testing Error Handling

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use horus::prelude::*;

    #[test]
    fn test_returns_not_found_for_missing_file() {
        let result = load_config("nonexistent.yaml");
        assert!(matches!(result, Err(HorusError::NotFound(_))));
    }

    #[test]
    fn test_returns_config_error_for_invalid_yaml() {
        let result = parse_config("invalid: [yaml");
        assert!(matches!(result, Err(HorusError::Config(_))));
    }

    #[test]
    fn test_error_context() {
        let err = HorusError::node("TestNode", "test message");
        let display = format!("{}", err);
        assert!(display.contains("TestNode"));
        assert!(display.contains("test message"));
    }
}
```

## Integration with anyhow

For applications that prefer `anyhow`:

```rust
use anyhow::{Context, Result as AnyhowResult};
use horus::prelude::*;

fn load_robot() -> AnyhowResult<Robot> {
    let config = load_config("robot.yaml")
        .context("Failed to load robot configuration")?;

    let robot = Robot::from_config(config)
        .context("Failed to create robot from config")?;

    Ok(robot)
}

// Convert back to horus::Result if needed
fn horus_function() -> Result<Robot> {
    load_robot().map_err(|e| HorusError::from(e))
}
```

## See Also

- [Core Concepts](/concepts/core) - Understanding HORUS nodes
- [API Reference](/rust/api/core) - Core API including Error types
- [Troubleshooting](/troubleshooting) - Common issues and solutions
