---
title: "HFrame (moved to Transform Frame)"
description: "Redirect stub — the transform frame documentation now lives at /concepts/transform-frame"
weight: 25
---

# HFrame has moved

`HFrame` was renamed `TransformFrame`, and this page moved with it. The documentation now lives at **[Transform Frame System](/concepts/transform-frame)** — this URL is kept only so older links keep resolving.

Everything that used to be here is on that page: adding `horus-tf` to `Cargo.toml`, the `Transform` type and its constructors, frame registration, name- and ID-based lookups, time-travel queries, configuration presets, the `horus frame` CLI commands, and diagnostics.
