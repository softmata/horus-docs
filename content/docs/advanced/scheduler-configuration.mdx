---
title: "Scheduler Configuration"
description: "Configuring the HORUS scheduler with the fluent node builder API, execution classes, and per-node settings"
weight: 40
---

# Scheduler Configuration

HORUS uses a single scheduler entry point — `Scheduler::new()` — with profile presets (`Scheduler::deploy()`, `Scheduler::hard_rt()`, `Scheduler::safety_critical()`) and a fluent node builder API that gives you full control over execution class, timing, ordering, and failure handling on a per-node basis.

## Creating a Scheduler

Every scheduler starts with `Scheduler::new()`. From there you can optionally set global parameters with builder methods before adding nodes:

```rust
use horus::prelude::*;

fn main() -> Result<()> {
    let mut scheduler = Scheduler::new()
        .tick_hz(1000.0)          // Global tick rate (default: 60 Hz)
        .with_capacity(32)        // Pre-allocate for 32 nodes
        .with_name("arm_control");// Name for logging/profiling

    // ... add nodes ...

    scheduler.run()?;
    Ok(())
}
```

### Builder Methods

| Method | Description | Default |
|--------|-------------|---------|
| `.tick_hz(hz)` | Global scheduler tick rate in Hz | `60.0` |
| `.with_capacity(n)` | Pre-allocate node storage | `16` |
| `.with_name(name)` | Human-readable scheduler name | `"default"` |
| `Scheduler::deploy()` | Production preset — safety + fault tolerance + blackbox | — |
| `Scheduler::hard_rt()` | Hard real-time preset — panics without RT capabilities | — |
| `Scheduler::safety_critical()` | Safety-critical preset — hard_rt + blackbox + profiling | — |

## Adding Nodes

Add nodes with `scheduler.add(n)`, then chain configuration calls, and finalize with `.build()?` (or its alias `.done()`):

```rust
use horus::prelude::*;

fn main() -> Result<()> {
    let mut scheduler = Scheduler::new()
        .tick_hz(1000.0);

    // Real-time motor control — runs first every tick
    // budget_us() auto-marks this node as RT
    scheduler.add(MotorController::new("arm"))
        .order(0)
        .budget_us(200)
        .deadline_ms(1)
        .on_miss(Miss::Skip)
        .build()?;

    // Sensor node — high priority, custom rate
    // rate() auto-derives budget and deadline, auto-marks as RT
    scheduler.add(LidarDriver::new("/dev/lidar0"))
        .order(10)
        .rate(500.hz())
        .build()?;

    // Compute-heavy planning — runs on a worker thread
    scheduler.add(PathPlanner::new())
        .order(50)
        .compute()
        .build()?;

    // Event-driven node — wakes only when the topic has new data
    scheduler.add(CollisionChecker::new())
        .on("lidar/points")
        .deadline_ms(5)
        .build()?;

    // Async I/O — network or disk, never blocks the real-time loop
    scheduler.add(TelemetryUploader::new())
        .order(200)
        .async_io()
        .rate_hz(10.0)
        .build()?;

    scheduler.run()?;
    Ok(())
}
```

## Execution Classes

Every node belongs to exactly one execution class. Set it in the builder chain:

| Method | Class | Description |
|--------|-------|-------------|
| `.compute()` | Compute | Offloaded to a worker thread pool. Use for planning, SLAM, or ML inference. |
| `.on(topic)` | Event-Driven | Wakes only when the named topic receives new data. |
| `.async_io()` | Async I/O | Runs on an async executor. Use for network, disk, or cloud calls. |

If no execution class is specified, the node defaults to **BestEffort**. A node is automatically promoted to the **RT** class when you set `.budget_us()`, `.budget()`, `.deadline_us()`, `.deadline_ms()`, or `.rate(Frequency)`.

### When to Use Each Class

- **RT (auto-detected)** — Motor controllers, safety monitors, sensor fusion, anything that must run every tick with bounded latency. Triggered by `.budget_us()`, `.budget()`, `.deadline_*()`, or `.rate(Frequency)`.
- **`.compute()`** — Path planning, point cloud processing, ML inference. These can take longer than a single tick without blocking RT nodes.
- **`.on(topic)`** — Collision detection, event handlers, reactive behaviors. Only runs when there is new data, saving CPU when idle.
- **`.async_io()`** — Telemetry upload, log shipping, cloud API calls. Never blocks any real-time or compute work.

## Per-Node Configuration

### Ordering and Timing

| Method | Description |
|--------|-------------|
| `.order(n)` | Execution priority within a tick (lower = runs first) |
| `.rate_hz(hz)` | Node-specific tick rate, independent of the global rate |
| `.rate(Frequency)` | Rate from `100.hz()` — auto-derives budget and deadline (auto-marks as RT) |
| `.budget_us(us)` | Tick budget in microseconds (auto-marks as RT) |
| `.budget(Duration)` | Tick budget as Duration, e.g. `200.us()` (auto-marks as RT) |
| `.deadline_ms(ms)` | Deadline in milliseconds from tick start |
| `.deadline_us(us)` | Deadline in microseconds from tick start |
| `.on_miss(Miss)` | What to do on deadline miss (`Miss::Warn`, `Miss::Skip`, `Miss::SafeMode`, `Miss::Stop`) |

### Failure Policy

| Method | Description |
|--------|-------------|
| `.failure_policy(policy)` | Per-node failure handling (see [Fault Tolerance](/advanced/circuit-breaker)) |
| `.build()` / `.done()` | Finalize and register the node (returns `HorusResult`) |

### Order Guidelines

- **0-9**: Critical real-time (motor control, safety)
- **10-49**: High priority (sensors, fast control loops)
- **50-99**: Normal priority (processing, planning)
- **100-199**: Low priority (logging, diagnostics)
- **200+**: Background (telemetry, non-essential)

## Global Configuration with Profile Presets

Instead of toggling individual flags, use profile presets that bundle the right settings for each deployment stage:

```rust
use horus::prelude::*;

// Development — lightweight
let mut scheduler = Scheduler::new()
    .tick_hz(1000.0)
    .with_profiling();

// Production — safety + fault tolerance + blackbox
let mut scheduler = Scheduler::deploy()
    .tick_hz(1000.0)
    .cores(&[2, 3]);

// Hard real-time — panics without RT capabilities
let mut scheduler = Scheduler::hard_rt()
    .tick_hz(1000.0);

// Safety-critical — hard_rt + blackbox + profiling
let mut scheduler = Scheduler::safety_critical()
    .tick_hz(1000.0)
    .max_deadline_misses(3);
```

## Python API

The Python API mirrors the Rust fluent builder:

```python
from horus import Scheduler

# Create scheduler with global settings
scheduler = Scheduler(tick_hz=1000.0, name="arm_control")

# Real-time motor controller (budget_us auto-marks as RT)
scheduler.add(motor_ctrl).order(0).budget_us(200).deadline_ms(1).on_miss("skip").build()

# Sensor with custom rate
scheduler.add(lidar).order(10).rate_hz(500.0).build()

# Compute node
scheduler.add(planner).order(50).compute().build()

# Event-driven node
scheduler.add(collision).on("lidar/points").deadline_ms(5).on_miss("safe_mode").build()

# Async I/O node
scheduler.add(telemetry).order(200).async_io().rate_hz(10.0).build()

scheduler.run()
```

You can also pass a config dict for global settings:

```python
scheduler = Scheduler(config={
    "tick_hz": 1000.0,
    "enable_profiling": True,
    "black_box_size_mb": 64,
})
```

Or use profile presets:

```python
scheduler = Scheduler.deploy(tick_hz=1000.0)
scheduler = Scheduler.hard_rt(tick_hz=1000.0)
scheduler = Scheduler.safety_critical(tick_hz=1000.0)
```

## Execution Modes

HORUS supports sequential and parallel execution. You configure this through `Scheduler::new()` and per-node execution classes.

<Callout type="info" title="Quick Answer">

**Use the default.** `Scheduler::new()` gives you deterministic, predictable execution that works for most robots. Start there and configure per-node execution classes as needed.

</Callout>

### Sequential Mode (Default)

Nodes execute one-by-one in priority order — same execution order every tick. Deterministic and certification-ready.

| Metric | Value |
|--------|-------|
| Latency | ~100-500ns per node |
| Deterministic | **Yes** |
| Multi-core | No (single thread) |
| Best For | Safety-critical, certification |

```rust,ignore
use horus::prelude::*;

// Safety-critical robot controller — 1 kHz tick
// budget_us() auto-marks nodes as RT
let mut scheduler = Scheduler::new().tick_hz(1000.0);
scheduler.add(safety_monitor).order(0).budget_us(100).build()?;
scheduler.add(controller).order(1).budget_us(200).build()?;
scheduler.run()?;
```

### Parallel Mode

Schedules independent nodes on different CPU cores. Nodes at the same `order` level run concurrently:

| Metric | Value |
|--------|-------|
| Latency | Variable (depends on workload) |
| Deterministic | No |
| Multi-core | **Yes** |
| Best For | Multi-sensor fusion, compute-heavy pipelines |

```rust,ignore
use horus::prelude::*;

let mut scheduler = Scheduler::new();

// These sensor nodes run in parallel (same order number)
scheduler.add(lidar_node).order(0).build()?;
scheduler.add(camera_node).order(0).build()?;
scheduler.add(imu_node).order(0).build()?;

// Fusion runs after all sensors (higher order number)
scheduler.add(fusion_node).order(1).build()?;
scheduler.run()?;
```

### Mode Comparison

| Feature | Sequential | Parallel |
|---------|------------|----------|
| Deterministic | **Yes** | No |
| Multi-core | No | **Yes** |
| Best Latency | 87-313ns | Variable |
| Certification Ready | **Yes** | No |

## See Also

- [Scheduling](/concepts/core-concepts-scheduler) - Scheduler overview and node ordering
- [Safety Monitor](/advanced/safety-monitor) - Safety monitoring and emergency stop
- [Fault Tolerance](/advanced/circuit-breaker) - Failure policies and recovery
- [Record & Replay](/advanced/record-replay) - Recording and playback
